/**
 * version.js — App version, shown in the top-right corner.
 * Bump this with every release so it's obvious when GitHub Pages
 * has picked up the latest commit.
 */
const APP_VERSION = 'v0.16.0';
